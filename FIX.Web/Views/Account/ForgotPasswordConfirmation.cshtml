@{
    ViewBag.Title = "Reset password request sent";
    Layout = "~/Views/Shared/_Portal.cshtml";
}
<div class="content-wrapper">
    <div class="content-header">
        <h1>@ViewBag.Title</h1>
        <hr />
        <h4>Please check your email to reset your password.</h4>
        <br/>
        <a href="@(Url.Action("Login"))" class="btn btn-default">Back to Login Page</a>
    </div>
</div>
